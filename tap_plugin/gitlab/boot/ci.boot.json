{
  "version": 1,
  "description": "gitlab CI record (req-boot-bootstrap-ci-record): the stack this plugin's own tests run in. Installs the depends_on closure (identity_core, aws_core; neither declares dependencies of its own), each from git at a pinned full commit, and gitlab itself; the consumer flips self to editable so the checkout under test runs, not the pinned revision. Population seeds gitlab's page bundle, which proves it imports. The only network access is the install of those pinned revisions; nothing seeded or tested reaches the network. Deliberately excluded: every other plugin and all credentials.",
  "install": {
    "plugins": [
      {
        "slug": "identity_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-identity-core",
          "rev": "53da388b6f47590090ef3bdc8d98731acff4c8e2"
        },
        "note": "Vocabulary dependency: owns identity_core__oidc_issuer and TRUSTS_ISSUER, which an OIDC sign-in provider points at, and identity_core__human and HELD_BY_HUMAN, which a user account points at. Pinned to the full commit of its main on 2026-09-24, the first carrying the human (no tagged release does yet; v0.1.2 predates it); move the pin deliberately."
      },
      {
        "slug": "aws_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-aws-core",
          "rev": "4e624769263f4e64bcce2c98643dc5d041639619"
        },
        "note": "Vocabulary dependency: owns ROUTES_TRAFFIC, which the /gitlab page's ingress search names, and the AWS types the page draws. Pinned to the full commit of its main on 2026-09-22 until it has a tagged release; move the pin deliberately."
      },
      {
        "slug": "gitlab",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/gitlab-tap",
          "rev": "3c8c00d33ebfebb60c69b599e3b38314ba0f25a4"
        },
        "note": "Self, pinned at the bootstrap commit until the first tagged release; the consumer (plugin-ci, or `spawn --dev-plugins gitlab`) substitutes the checkout under test via tap.dev_workspace (req-boot-bootstrap-ci-record-4)."
      }
    ]
  },
  "population": {
    "on_failure": "abort",
    "steps": [
      {
        "type": "seed-plugin",
        "plugin": "gitlab",
        "enabled": true,
        "note": "gitlab's own bundle: the /gitlab page, its panels, searches and projection."
      }
    ]
  }
}
