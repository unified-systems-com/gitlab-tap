{
  "version": 1,
  "description": "gitlab CI record (req-boot-bootstrap-ci-record): the stack this plugin's own tests run in. Installs gitlab alone (it declares no dependencies); the consumer flips self to editable so the checkout under test runs, not the pinned revision. Population is empty: v0 ships no GRIFT. Deliberately excluded: every other plugin, all credentials, anything that reaches the network.",
  "install": {
    "plugins": [
      {
        "slug": "gitlab",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/gitlab-tap",
          "rev": "3c8c00d33ebfebb60c69b599e3b38314ba0f25a4"
        },
        "note": "Self, pinned at the bootstrap commit until the first tagged release; the consumer (plugin-ci, or `spawn --dev-plugins gitlab`) substitutes the checkout under test via tap.dev_workspace (req-boot-bootstrap-ci-record-4)."
      }
    ]
  },
  "population": {
    "on_failure": "abort",
    "steps": []
  }
}
