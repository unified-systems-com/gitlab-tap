{
  "slug": "REGISTERS_RUNNER__gitlab",
  "name": "Registers Runner",
  "description": "An instance, group or project has this runner registered to it, which is what lets the runner take its jobs. A project runner can be enabled on further projects; the first is its owner.",
  "sources": [
    "gitlab__gitlab_instance",
    "gitlab__gitlab_group",
    "gitlab__gitlab_project"
  ],
  "targets": [
    "gitlab__gitlab_runner"
  ],
  "property_schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "assignment": {
        "type": "string",
        "enum": [
          "owner",
          "enabled"
        ],
        "description": "owner: the scope the runner was created in; enabled: a further project it was enabled on."
      }
    }
  },
  "default_dimensions": {
    "gitlab.plane": "application"
  }
}
