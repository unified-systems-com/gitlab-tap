{
  "slug": "READS_SECRET__gitlab",
  "name": "Reads Secret",
  "description": "A GitLab process reads a secret at start-up or at runtime (the database password, gitlab-secrets.json, a runner token). The target is open: a Secrets Manager secret (aws_core__aws_secrets_manager_secret) here.",
  "sources": [
    "gitlab__gitlab_component",
    "gitlab__gitaly_node",
    "gitlab__runner_manager"
  ],
  "property_schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "purpose": {
        "type": "string",
        "description": "What the secret is for (for example 'database password', 'runner authentication token')."
      },
      "delivery": {
        "type": "string",
        "enum": [
          "env_var",
          "file",
          "api"
        ],
        "description": "How the process receives it."
      }
    }
  },
  "default_dimensions": {
    "gitlab.plane": "infrastructure"
  }
}
